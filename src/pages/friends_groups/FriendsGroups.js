import './FriendsGroups.css';

import BaseLayoutPage from './../../components/base_layout_page/BaseLayoutPage';

const FriendsGroups = ({ isSidebarOpen, toggleSidebar }) => {
    return (
        <BaseLayoutPage title={'Grupo de Amigos'} isSidebarOpen={isSidebarOpen} toggleSidebar={toggleSidebar} >
            asdasdas
        </BaseLayoutPage>
    )
}

export default FriendsGroups;